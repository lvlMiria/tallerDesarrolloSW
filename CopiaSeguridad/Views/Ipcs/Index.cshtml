@model IEnumerable<Presentacion.Models.Ipc>

@{
    ViewData["Title"] = "Valor IPC";
}

<div class="card-body m-5" style="background-color:white; border-radius: 5px">
    <div class="container-fluid d-flex flex-column align-items-center">
        <div class="row">
            <h3>Valor IPC</h3>
        </div>
    </div>

    <div class="container-fluid d-flex flex-column align-items-center">
        <div class="card" style="width: 30rem;border-color:forestgreen">
            <div class="card-body">
                <form asp-action="ObtenerValor" method="POST">
                    @*Valor*@
                    <div class="row m-1">
                        <label for="txtValor" class="form-label">Valor</label>
                        <div class="input-group mb-3">
                            <input type="text" value="@ViewBag.Mostrar.Valor" class="form-control" aria-label="txtValor">
                        </div>
                    </div>
                    @*Año*@
                    <div class="row m-1">
                        <label for="txtAnio" class="form-label">Año</label>
                        <div class="input-group mb-3">
                            <input type="text" value="@ViewBag.Mostrar.Anio" class="form-control" aria-label="txtAnio" name="txtAnio">
                            <button class="btn btn-outline-secondary" type="submit" id="obtenerValor">
                                <span class="material-icons">
                                    search
                                </span>
                            </button>
                        </div>
                        <span class="alert-danger">@ViewBag.Error</span>
                    </div>
                    @*Botones*@
                    <div class="row" style="margin-top:40px">

                        <div class="col-12 col-md-4">
                            <div class="m-2">
                                <a asp-action="Index" asp-controller="Home">
                                    <div class="d-grid gap-2">
                                        <button type="button" style=" background-color: forestgreen; color: white;" class="btn">Salir</button>
                                    </div>
                                </a>
                            </div>
                        </div>

                        <div class="col-12 col-md-4">
                            <div class="m-2">
                                <div class="d-grid gap-2">
                                    <button type="button" style=" background-color: forestgreen; color: white;" class="btn">Actualizar</button>
                                </div>
                            </div>
                        </div>

                        <div class="col-12 col-md-4">
                            <div class="m-2">
                                <a asp-action="Create">
                                    <div class="d-grid gap-2">
                                        <button type="button" style=" background-color: forestgreen; color: white;" class="btn">Nuevo</button>
                                    </div>
                                </a>
                            </div>
                        </div>

                    </div>
                </form>
            </div>
        </div>

    </div>

    @* <div class="container-fluid d-flex flex-column align-items-center">

        <div class="row m-1">
            <p style="font-weight:bold">Histórico</p>
        </div>
        <div class="col">
            @foreach (var item in Model)
            {
                <p>
                    @Html.DisplayFor(modelItem => item.Anio): @Html.DisplayFor(modelItem => item.Valor)% | @Html.ActionLink("Actualizar", "Edit", new {id = item.Anio })
                </p>
            }
        </div>
    </div> *@

</div>



@* ================ Original ================
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Anio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Anio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Anio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
*@